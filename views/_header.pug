// HEADER
header.header
  nav.nav.nav-tours
    a.nav__el(href='/') All Tours
  .header__logo
    img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user
  
    //- user variable came from authController.isLoggedIn (res.locals.user = currentUser;)
    //- It is better to avoid conditionals in pug templates. However, we use them 
    //- for rendering pages (to display different content of the page for logged in users) 
    if user 
      a.nav__el.nav__el--logout Log out
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
        
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='/signup') Sign up
